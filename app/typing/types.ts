export enum Decision {
    HIGHER = 'higher',
    LOWER = 'lower'
};

export interface DelayInfo {
    name: string,
    eva: number,
    delay: number,
    photoName: string,
    photographer: string
    license: string
};