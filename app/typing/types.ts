export enum Decision {
    HIGHER = 'higher',
    LOWER = 'lower'
};

export interface DelayInfo {
    name: string,
    eva: number,
    delay: number,
    photoUrl: string,
    photographer: string
    license: string
};